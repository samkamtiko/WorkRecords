package controller;

import http.HttpRequest;
import http.HttpResponse;

public class UsersNewHandler extends GenericHandler {

    @Override
    public HttpResponse handlePostRequest(HttpRequest request) {
        return super.handlePostRequest(request);
    }
}